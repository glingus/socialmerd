#!/usr/bin/env node
// Fase 7: bump di versione, build di tutte le varianti, tag git e note per
// la Release GitHub. Il push su main e la creazione della release restano
// sempre annunciati/confermati dall'utente (repo pubblico).
console.log('TODO (Fase 7 — Release v1.0.0): non ancora implementato.');
console.log('Vedi docs/PIANO.md §5.7.');
process.exit(1);
