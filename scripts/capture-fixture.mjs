#!/usr/bin/env node
// Fase 2: apre l'URL passato come argomento nel profilo autenticato da
// e2e-login.mjs, salva l'HTML sanificato (nomi, testi, URL media sostituiti,
// script/JSON rimossi) sotto tests/fixtures/ e l'originale non sanificato
// sotto tests/fixtures/raw/ (ignorato da git, mai committato).
console.log('TODO (Fase 2 — Spike sul DOM reale): non ancora implementato.');
console.log('Vedi docs/PIANO.md §5.2.');
process.exit(1);
